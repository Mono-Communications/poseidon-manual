# 15. 아웃바운드 방화벽 안내

Poseidon 연동에서 고객사가 유일하게 **네트워크 작업**을 해야 하는 부분이며, 연동 실패의 대부분이 여기서 발생합니다. 이 페이지 내용은 고객사 보안·네트워크 담당자에게 그대로 전달할 수 있습니다.

## <mark style="color:blue;">왜 아웃바운드가 필요한가</mark>

***

Poseidon은 고객사 서버 상태를 **가져오는(pull) 방식이 아니라, 엔진이 보내는(push) 방식**으로 수집합니다. 즉 고객사에서 나가는 경로가 열려 있지 않으면 감시 기능이 전혀 동작하지 않습니다.

이 구조 덕분에 보안 관점에서 아래가 보장됩니다.

| 항목             | 내용                                   |
| -------------- | ------------------------------------ |
| **통신 방향**      | 고객사 → Poseidon **단방향**               |
| **인바운드 포트 개방** | **불필요** — Poseidon이 고객사로 접속하는 경로가 없음 |
| **원격 제어**      | 없음 — 명령 전달·설정 변경 기능이 없음              |
| **VPN / 전용선**  | 불필요 — HTTP(S) 아웃바운드만으로 동작            |
| **대상 범위**      | 지정된 단일 호스트·포트 1개소로 한정                |
| **고객사 DB 접근**  | 없음                                   |

## <mark style="color:blue;">IP, Port 설정 안내</mark>

전달 내용 4가지:

<table><thead><tr><th width="305.25">전달물</th><th>내용</th></tr></thead><tbody><tr><td><strong>접속 주소</strong></td><td><strong>ip: </strong><mark style="color:red;"><strong>4.217.193.23</strong></mark><strong> / port: </strong><mark style="color:red;"><strong>8181</strong></mark></td></tr><tr><td><strong>인증 키</strong></td><td>발급된 API Key (<strong>노드별로 어느 서버용인지 명시</strong>)</td></tr></tbody></table>

고객이 넣어야 하는 설정 (`config/system.yml`):

```yaml
poseidon:
  enabled: true
  url: http://{{ip}}:{{port}}/api/v1/odyssey-instances
  auth-key: {{발급받은 인증 키}}
  contact-email: {{담당자 이메일}}
  heartbeat:
    interval-ms: 30000
  cypher:
    enabled: true
    coalesce-window-ms: 30000
```

{% hint style="info" %}
안내할 때 **재시작이 필요하다**는 점을 반드시 명시하십시오.
{% endhint %}

<table><thead><tr><th width="216">옵션</th><th width="122">기본값</th><th>설명</th></tr></thead><tbody><tr><td><strong><code>poseidon.enabled</code></strong></td><td><mark style="color:red;"><code>true</code></mark></td><td>연동 활성화 여부 <mark style="color:red;"><code>false</code></mark>면 <mark style="color:red;"><code>url</code></mark> 설정과 무관하게 전송을 전면 중단</td></tr><tr><td><strong><code>poseidon.url</code></strong></td><td>-</td><td>Poseidon API 엔드포인트 URL. (운영 환경 별로 별도 발급)</td></tr><tr><td><strong><code>poseidon.auth-key</code></strong></td><td>-</td><td>Poseidon 인증 키. (인스턴스별 발급된 값을 입력)</td></tr><tr><td><strong><code>poseidon.contact-email</code></strong></td><td>-</td><td>장애 발생 시 연락받을 담당자 이메일 (고객사)</td></tr><tr><td><strong><code>heartbeat.interval-ms</code></strong></td><td><mark style="color:red;"><code>10000(10초)</code></mark></td><td>하트비트 전송 주기</td></tr><tr><td><strong><code>cypher.enabled</code></strong></td><td><mark style="color:red;"><code>true</code></mark></td><td>오류 자동 수집(내장 Cypher) 사용 여부</td></tr><tr><td><strong><code>cypher.coalesce-window-ms</code></strong></td><td><mark style="color:red;"><code>8000(8초)</code></mark></td><td>동일 오류 폭주 시 묶음 처리 윈도</td></tr></tbody></table>

{% hint style="info" %}
<mark style="color:red;">`url`</mark>, <mark style="color:red;">`auth-key`</mark> 운영 대시보드 접속 정보는 운영 환경별로 다르게 발급됩니다. 정확한 값은 별도 안내드립니다. 인증 키는 인스턴스 단위로 발급되며 재발급 시 기존 키는 폐기되므로, 설정 파일 외부로 노출되지 않도록 관리해 주십시오.
{% endhint %}

{% hint style="info" %}
연동을 사용하지 않는 경우 `poseidon.enabled: false` 로 설정하거나 `poseidon` 블록을 주석 처리하면 됩니다. \
이 상태에서는 Poseidon 으로의 외부 호출이 일절 발생하지 않습니다.
{% endhint %}

{% hint style="info" %}
Poseidon 연동을 사용하는 경우 **고객사 방화벽에 Poseidon 서버 IP를 등록**해 주셔야 합니다. Odyssey가 주기적으로 외부의 Poseidon 클라우드(`poseidon.url` 항목에 명시된 호스트)로 HTTP요청(하트비트, 알림)을 송신해야 하므로 outbound 통신이 차단되어 있는 경우 연동이 동작하지 않습니다.&#x20;
{% endhint %}

## <mark style="color:blue;">차단되어 있으면 잃는 것</mark>

***

발송·수신은 계속 동작하지만 **모니터링 기능이 전부 무력화**됩니다.

| 잃는 것            | 구체적 영향                                          |
| --------------- | ----------------------------------------------- |
| **생존 감시**       | 엔진이 중단되어도 알 수 없음 → 장애 인지가 고객 문의 시점까지 지연         |
| **지표 축적**       | 큐 적체·응답 지연·실패율 상승 같은 **선행 징후 관측 불가** → 선제 조치 불가 |
| **오류 수집·분석**    | 예외 이력이 남지 않음 → 사후 원인 분석도 고객사 로그를 직접 받아야만 가능     |
| **장애 알림**       | 담당자 알림 미발송                                      |
| **복구 시간(MTTR)** | 근거 데이터가 없어 추정에 의존 → 원인 규명·복구 지연                 |

{% hint style="warning" %}
**부분 차단(간헐적 드롭)이 더 골치 아픕니다.** 정상 가동 중인데도 하트비트 미수신으로 `DOWN` 오탐이 반복되어 알림 신뢰도가 떨어집니다. [5. 이벤트](../part-2/5.-events.md)에서 `하트비트 타임아웃` ↔ `복구`가 반복되는 패턴으로 바로 확인됩니다.
{% endhint %}

## <mark style="color:blue;">차단 상태여도 발송에는 영향 없음</mark>

***

Poseidon 전송은 발송 파이프라인과 분리된 전용 스레드에서 수행되며, 실패 시 경고 로그만 남기고 넘어갑니다. **방화벽이 닫혀 있거나 Poseidon이 점검 중이어도 발송은 정상 진행**됩니다. 다만 그 기간의 지표·오류 이력은 유실됩니다.

이 점을 먼저 말해주면 고객 보안팀의 경계심이 크게 낮아집니다.

## <mark style="color:blue;">**고객 로그로 판별하기**</mark>

***

고객사 엔진 로그에서 아래 문자열을 확인하면 방화벽 문제인지 바로 구분됩니다.

| 로그                                                     | 판정                    |
| ------------------------------------------------------ | --------------------- |
| **`[Poseidon - heartbeat] OK`**                        | 정상 — 경로 열려 있음         |
| **`[Poseidon - heartbeat FAIL] Connection timed out`** | **아웃바운드 차단 의심**       |
| **`[Poseidon - heartbeat FAIL] No route to host`**     | **아웃바운드 차단 의심**       |
| **`[Poseidon - start FAIL] status=401`**               | 경로는 열림, **인증 키 문제**   |
| **`[Poseidon - start FAIL] status=404`**               | 경로는 열림, **URL 경로 오류** |
| 로그가 아예 없음                                              | 연동 비활성 상태 또는 재시작 안 함  |

{% hint style="success" %}
**FAIL 로그에 HTTP 상태 코드가 찍혀 있으면 방화벽은 열린 것입니다.** 상태 코드가 없고 timeout·no route 계열이면 네트워크 차단입니다. 이 한 가지 기준만 알아도 문의의 절반은 즉시 분류할 수 있습니다.
{% endhint %}

## <mark style="color:blue;">**HA 이중화 주의**</mark>

***

* 두 노드가 **각각 독립적으로** 전송합니다 → 방화벽도 양쪽 다 필요
* 한쪽만 열면 [4. 인스턴스](../part-2/4.-instances.md) 화면에 인스턴스 1개만 `UP`으로 보입니다
* "1대는 되는데 1대는 안 된다"는 문의는 거의 항상 **한쪽 노드 방화벽 누락**입니다
* 확인 방법: [6. 고객사](../part-2/6.-tenants.md) 상세의 **인스턴스 헬스맵**에서 카드 수가 노드 수와 맞는지 → 인스턴스 화면에서 몇 개가 `UP`인지 대조
